spring:
  application:
    name: demo-graphql
  graphql:
    graphiql:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5433/demo_graphql
    username: demo
    password: demo
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
  docker:
    compose:
      file: compose.yaml
management:
  endpoints:
    web:
      exposure:
        include: health
logging:
  level:
    root: INFO
    sql: DEBUG
